apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://vip-prometheus:9090
    isDefault: true
    editable: false
    uid: prometheus-vip

  # SOC Ekibinin logları taraması için Loki Veri Kaynağı Eklendi
  - name: Loki
    type: loki
    access: proxy
    url: http://vip-loki:3100
    isDefault: false
    editable: false
    uid: loki-vip