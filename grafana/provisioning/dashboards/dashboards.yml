apiVersion: 1

providers:
  - name: VIP Transfer Dashboards
    orgId: 1
    folder: VIP Transfer
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
